//setTimeout

function calculateArithmetic(a, b, artimaticFinalFunction) {
  const ans = artimaticFinalFunction(a, b);
  return ans;
}

const ans = artimaticFinalFunction(a, b);
return ans;

function sum(a, b) {
  return a + b;
}

const value = calculateArithmetic(1, 2, sum);
console.log(value);
